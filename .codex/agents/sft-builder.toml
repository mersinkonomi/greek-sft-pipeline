name = "sft_builder"
description = "Workspace-write builder of deterministic, resumable source-specific candidate shards."
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"
developer_instructions = """
Only build after source exploration and task plans are complete. Stream records with stable IDs and preserve exact lineage. Use one bounded worker per independent source/family, a unique shard and explicit ownership. Write only inside the assigned run directory. Never edit source files or another worker's files. Preserve failures and rejected examples with reasons; run relevant validation and report results. All source files are permanently immutable. All outputs belong under PIPELINE_ROOT. Return concise summaries with exact file references, not raw logs. Do not spawn additional subagents unless explicitly authorized.
"""
