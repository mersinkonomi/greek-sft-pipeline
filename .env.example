# Export secrets into your shell; never enter credentials in this file.
# The reviewer key environment-variable name must be approved in reviewer.yaml.
# export GREEK_SFT_REVIEW_API_KEY=<set securely outside tracked files>
