# Greek SFT pipeline instructions

The primary agent orchestrates the work, owns the final response, and waits for required subagents. Explore before implementing; use bounded parallel read-only investigations and independent source workers. Review implementation and test coverage before declaring completion. Skip unnecessary delegation for trivial tasks.

SOURCE_ROOT is `/datadisk2/greekllm/GreekLLM_jsonl` and is permanently immutable. Open source files only for reading. Never create, edit, rename, delete, truncate, move, chmod, normalize in place, or execute scripts there. All code, caches, temporary files, logs, tests and artifacts must remain under `/datadisk2/greekllm/GreekLLM_sft_pipeline`. Verify canonical paths and reject symlink output escapes. Never overwrite an existing run.

Only one agent may edit any particular file. The orchestrator assigns explicit ownership. Runtime builders write only unique shards inside their assigned run directory. Validators write new artifacts and never alter raw candidates. Preserve revisions and all review history. Subagents return concise evidence, exact paths and test results; they must not spawn additional subagents unless explicitly authorized.

Use exactly six passes and five checkpoints: inventory; derived cleaning and source plans; raw candidates; deterministic validation; ordered ExactSubstrings, MinhashDedup, SentenceDedup, balancing and grouped splits; then separately approved API review and release audit. Checkpoints retain data, accounting, statistics, manifests, checksums, configuration snapshots and errors. Reconcile every source file and every identifiable source record. Unknown record boundaries must remain explicitly unresolved, never claimed as complete record coverage.

The user authorized continuing all-source auditing after a licensing/privacy preflight blocker. Quarantine unresolved licenses, sensitive data and unsupported or ambiguous schemas; this authorization does not establish a license or permit releasing restricted content. Record precise reasons. Source hash changes, unsafe paths, excessive processing errors and unapproved API volume remain stop conditions.

API review is disabled until the user supplies configuration and external-data permissions. Never print or persist credentials. Estimate volume and budget before the canary; obtain separate approval before full review. No pending or unaccepted review can enter a release. All release gates must pass, including evaluation contamination checks, source immutability and licenses. Call an eligible result a release-grade Greek SFT candidate until final human and legal approval.

The user subsequently instructed “go past greek training”. The run-local inventory scope amendment excludes further record parsing of the exact top-level `greek_training` directory and prioritizes other source collections. Retain previously committed accounting, inventory/hash every original file read-only, and verify all hashes after processing. Unscanned excluded records remain explicitly unassessed; never claim complete record coverage for this scope. Preserve the original frozen configuration and the separately checksummed scope amendment.

After a confirmed SHA-256 change in `greek_training/.git/index`, the user approved excluding the exact `greek_training/.git/` subtree from source coverage and resuming with that exception documented. This later authorization narrows the prior all-file hash requirement only for that subtree. Preserve the failed state, original hashes, historical file/record accounting, integrity incident, frozen configuration and first amendment. A separate checksummed source-exclusion amendment binds the authorization to this run and incident. Do not open or traverse the excluded Git directory. Verify every remaining source file, and continue to stop on any change outside the exact exclusion. Report scoped integrity separately from the original full-tree integrity failure; never relabel excluded files as unchanged or claim complete current coverage of the excluded directory. Source files remain permanently read-only, including excluded files.

The user later clarified: “greek training is a mix of the others so we dont make sft from that”. The exact top-level `greek_training/` collection is now excluded from SFT generation and current source coverage, including its non-Git files. This recorded instruction authorizes the checksummed `derived_collection_exclusion_amendment.json`, which preserves and references the earlier Git-only amendment and both integrity incidents. Preserve all historical inventory, record ranges and original hashes; do not open or traverse the excluded collection. Path scope overrides historical family labels. Continue processing original collections outside this root with all source-protection checks; `greek_training_temp` remains in scope. Historical excluded counts are not current coverage, and full-original-tree integrity remains failed. No source is modified or rebaselined.

The user subsequently answered “leave it continue with the other ones” to the explicit proposal to exclude `greek_training_temp/` from SFT generation and current source coverage. This authorizes the exact `greek_training_temp/` root alongside the already excluded `greek_training/` root. Record the decision and new integrity incident in a separately checksummed temporary-collection amendment preserving the full predecessor chain. Do not open or traverse either excluded root. Preserve completed Passes 1–4 and their frozen identities byte-for-byte; reuse requires a separate checksummed scope reconciliation proving no accepted disposition or candidate refers to either excluded root and recomputing both-root historical accounting. A fresh verifier must check every remaining in-scope source. Historical and preflight excluded observations are not current excluded coverage. Source files remain read-only, no baseline is replaced, and API/release approvals remain separate.
